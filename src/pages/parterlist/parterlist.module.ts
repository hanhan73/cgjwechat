import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { ParterlistPage } from './parterlist';

@NgModule({
  declarations: [
    ParterlistPage,
  ],
  imports: [
    IonicPageModule.forChild(ParterlistPage),
  ],
})
export class ParterlistPageModule {}
